import React, { FC } from "react";

import { Container } from "@material-ui/core";

interface Props {}

const ListState: FC<Props> = props => {
  const {} = props;

  return <Container maxWidth="lg">here</Container>;
};

export default ListState;
